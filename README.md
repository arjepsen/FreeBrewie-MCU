# FreeBrewie MCU

ATmega2560 firmware for the FreeBrewie / ReBrewie project.

The MCU owns hardware execution, measurements, interlocks, faults, startup gating, and shutdown behavior.

The SOM owns UI, recipes, cleaning flows, manual-service workflows, and timing.

This firmware is built with PlatformIO.